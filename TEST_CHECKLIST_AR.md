# قائمة اختبار عملي — Web Cost App

**آخر تحديث الوثيقة:** 2026-05-19  
**مرجع تقني:** `CONTEXT.md` · `CLAUDE.md`

> علّم كل بند: `[ ]` لم يُختبر · `[x]` ناجح · `[~]` يحتاج مراجعة · `[!]` فشل

---

## متطلبات قبل الاختبار

### بيئة Hybrid (مخازن + مواد)

```powershell
cd "G:\cost web app\web-cost-app"
npm run local:bootstrap-admin   # مرة واحدة
npm run local:api               # Terminal 1 — المنفذ 3001
npm run dev                     # Terminal 2 — Vite (3000 أو التالي)
```

| المتغير | القيمة المتوقعة |
|---------|-----------------|
| `VITE_DATA_BACKEND` | `local` |
| `VITE_API_BASE_URL` | `/api` |

بدون تشغيل `local:api`: وحدة **المخازن** وشجرة الأصناف والفواتير الموزعة **لن تعمل**.

### حسابات مقترحة للاختبار

| الحساب | الدور | الغرض |
|--------|-------|--------|
| Admin | `admin` | مسار كامل + استيراد Excel |
| PM | `projects_manager` | BOQ، اعتماد تحويلات |
| Accountant | `project_accountant` + `assignedContractIds` | تقييد العقود + قراءة مرجعية |
| Pending | `user` بدون صلاحيات | شاشة «بانتظار التفعيل» |
| غير مسجّل محلياً | Google بدون مستخدم SQLite | رسالة ربط الجلسة المحلية |

---

## بيانات جلسة الاختبار

| الحقل | القيمة |
|-------|--------|
| اسم المختبر | |
| تاريخ الاختبار | |
| فرع / commit | |
| البيئة | محلي / Firebase إنتاج / أخرى |
| المتصفح والجهاز | |
| وضع البيانات | `VITE_DATA_BACKEND=local` / Firestore فقط |
| البريد المستخدم | |
| الدور | Admin / Projects Manager / Project Accountant / User |
| منفذ Vite | |
| `local:api` يعمل؟ | نعم / لا |

**ملاحظات قبل البدء:**

---

## ملخص النتيجة

| المؤشر | العدد |
|--------|-------|
| إجمالي البنود المختبرة | |
| ناجح `[x]` | |
| مراجعة `[~]` | |
| فشل `[!]` | |

- **أخطر مشكلة:**
- **جاهز للاستخدام العملي؟** نعم / بشرط / لا

---

## سجل الأخطاء

```text
رقم:
الوحدة:
الخطورة: حرجة / عالية / متوسطة / منخفضة
الخطوات:
المتوقع:
الفعلي:
بيانات الاختبار:
يتكرر؟ نعم / لا
```

---

## قضايا معروفة — أعد التحقق بعد الإصلاح

| # | الوحدة | الوصف | آخر تحقق |
|---|--------|--------|----------|
| K1 | دخول محلي | حساب Google غير موجود في SQLite → رسالة «لا يوجد مستخدم محلي…» وإخفاء الموديولات | |
| K2 | صلاحيات | محاسب مشروع: عدم ظهور COA/مشاريع/عقود في فاتورة التكاليف رغم الحاجة المرجعية (`canRead*Ref`) | |
| K3 | GL | بحث الحسابات: نتائج غير مطابقة (مثلاً بحث `11102` يظهر `51104001`) | |
| K4 | استقرار | خطأ في وحدة يعيد التطبيق للموديول الافتراضي بدل `WindowErrorBoundary` فقط | |

---

## 1. التشغيل والدخول

- [× ] فتح التطبيق بدون شاشة بيضاء أو أخطاء Console حرجة.
- [ ×] شاشة الدخول — Google فقط (لا تسجيل بريد/كلمة مرور).
- [ ×] تسجيل الدخول بحساب مصرح في Firestore `users/{uid}`.
- [ ×] **محلي:** بعد Google، نجاح `POST /api/auth/firebase-session` (تحقق Network :3001).
- [ ×] **محلي:** حساب Google **غير** موجود محلياً → رسالة واضحة وعدم فتح موديولات عمل.
- [ ×] مستخدم Firestore بدون صلاحيات → شاشة **«الحساب بانتظار التفعيل»** (لغة + خروج فقط).
- [ ×] فتح الموديول الافتراضي (`defaultModule` من Firestore، مع `normalizeDefaultModule()`).
- [ ] إعادة تحميل الصفحة تحافظ على الجلسة.
- [ ×] تسجيل الخروج يعيد شاشة الدخول.
- [ ] انتهاء الجلسة: لا حفظ صامت لعمليات مالية (رسالة خطأ من `accountingService`).

---

## 2. الواجهة العامة والتنقل

- [ ] الشريط الجانبي يعرض الموديولات حسب `moduleAccess` فقط.
- [ ] فتح عدة نوافذ؛ إغلاق واحدة لا تغلق الباقي.
- [ ] **إغلاق الكل** مع تأكيد.
- [ ] حالات الشريط: نشط (أزرق) / مفتوح (نقطة) / مغلق.
- [ ] تنقل لوحة المفاتيح `↑`/`↓` بين عناصر الشريط.
- [ ] **Alt+Tab** داخل التطبيق يدوّر النوافذ حسب z-index.
- [ ] تبديل اللغة ar/en — RTL/LTR صحيح.
- [ ] ثيم Dark / Soft / Light على الواجهة والنوافذ.
- [ ] حفظ اللغة (`localStorage`) والثيم (`localStorage` + Firestore `defaultTheme`).
- [ ] **إعدادات العرض** (أيقونة Palette في أسفل الشريط): حفظ `defaultTheme` و`defaultModule`.
- [ ] **الآلة الحاسبة** (أيقونة Calculator): نافذة 240×400، لوحة مفاتيح، تاريخ، Ctrl+C/V.
- [ ] إعادة فتح الآلة الحاسبة تجلبها للأمام دون تصغير النوافذ الأخرى.
- [ ] Toasts نجاح/خطأ واضحة.
- [ ] `WindowErrorBoundary`: خطأ في موديول واحد → إعادة محاولة/إغلاق **دون** سقوط التطبيق بالكامل.

---

## 3. الصلاحيات والأدوار

اختبر **Admin** + **Projects Manager** + **Project Accountant** (بعقود مسندة) + **User**.

- [ ] Admin: كل الموديولات وكل العمليات.
- [ ] Projects Manager: مشاريع، BOQ، billing (عرض تكاليف)، اعتماد تحويلات، **لا** إعدادات مستخدمين الحساسة.
- [ ] Project Accountant: تكاليف، مخازن، billing — **عقود `assignedContractIds` فقط**.
- [ ] User: لوحة فقط حتى منح الصلاحيات من الإعدادات.
- [ ] إخفاء موديول من الشريط ≠ منع قراءة بياناته **المرجعية** في موديول مسموح (COA، مشاريع، عقود، BOQ، موردين) — راجع K2.
- [ ] منع Create/Update من الواجهة عند غياب صلاحية CRUD.
- [ ] **محلي:** نفس المنع عبر API (`403` من `requirePermission`).
- [ ] الإعدادات: حفظ دور + صلاحيات + `assignedContractIds`.

---

## 4. لوحة التحكم

- [ ] بطاقات الملخص بدون NaN أو سالب غير مبرر.
- [ ] عدد المشاريع = غير المحذوفة (`isDeleted`).
- [ ] **الدفعات المقدمة** في التحصيل النقدي **ولا** تُخصم من `pendingBilling`.
- [ ] `pendingBilling = totalRevenue - ipcCollected` (ليس totalCollected).
- [ ] كشف نقد/بنك: حسابات تبدأ بـ `121`.
- [ ] مخططات مع/بدون بيانات.
- [ ] تنبيه عند الوصول لحد `dataLimits` على المعاملات.
- [ ] تحديث قيود/مستخلصات ينعكس على اللوحة.

---

## 5. الأستاذ العام

### شجرة الحسابات

- [ ] تحميل 5 مستويات.
- [ ] زر `+` على حساب تجميعي فقط (`isGroup === true`).
- [ ] توليد كود تسلسلي 8 أرقام تحت الأب.
- [ ] بحث/تصفية — دقة المطابقة (راجع K3).
- [ ] تعديل لا يكسر قيوداً تاريخية.

### القيود اليومية

- [ ] قيد يدوي متوازن (تسامح ±0.005).
- [ ] رفض قيد غير متوازن.
- [ ] اختيار حساب يملأ الاسم تلقائياً (استدعاء واحد لـ `handleEntryChange`).
- [ ] `costCenterId` = عقد؛ `projectId` من العقد وليس من العقد كمشروع.
- [ ] حذف منطقي `isDeleted`.
- [ ] فلتر السنة المالية + **تحميل المزيد** بدون تكرار.
- [ ] كشف حساب: افتتاحي + حركات + ختامي.
- [ ] **تسوية عهدة** (من التكاليف أو GL): قيود متوازنة حسب عقد.

---

## 6. المشاريع والعقود

- [ ] إنشاء/تعديل مشروع؛ حقول إلزامية.
- [ ] حالات المشروع: نشط / مكتمل / موقوف / ملغي.
- [ ] حذف منطقي للمشروع.
- [ ] عقد مرتبط بمشروع؛ منع عقد بدون مشروع.
- [ ] العقود كمراكز تكلفة في GL والتكاليف والمستخلصات.
- [ ] **محلي:** أسفل المشاريع — **شجرة الأصناف** (`MaterialsTree`) تظهر وتعمل.

---

## 7. شجرة الأصناف (محلي)

**الموقع:** أسفل **المشاريع** · تبويب **Materials** في **المخازن**.

- [ ] إضافة مجموعة يدوياً.
- [ ] إضافة صنف (كود، اسم، وحدة) تحت مجموعة.
- [ ] **تصدير** Excel — كل المجموعات والأصناف.
- [ ] **قالب** (admin / projects_manager).
- [ ] **استيراد** — صفوف جديدة تُضاف؛ أكواد مكررة **تُتخطى** (لا تحديث in-place).
- [ ] أعمدة عربي أو إنجليزي: كود/اسم مجموعة، كود/اسم صنف، وحدة.
- [ ] صف بدون كود صنف = مجموعة فقط.
- [ ] رفض ملف Excel بتنسيق خاطئ برسالة واضحة.

---

## 8. جداول الكميات BOQ

- [ ] اختيار مشروع → عقد → بنود العقد فقط.
- [ ] نموذج بند: فصل → نوع عمل → قسم → بند (قوائم متدرجة + «جديد»).
- [ ] توليد كود البند التالي داخل القسم.
- [ ] إجمالي = كمية × سعر وحدة.
- [ ] حذف منطقي؛ استيراد Excel (`cellDates: true`) للتواريخ.
- [ ] مسح/استيراد جماعي ذري (`writeBatch`).
- [ ] تقدم من `billing` (submitted/approved/paid)؛ حالات: لم يبدأ / جاري / متأخر / منتهي.
- [ ] **محلي:** أعمدة تكلفة شراء ورصيد مخزون (`boq-actuals`).
- [ ] **محلي:** زر ربط المواد → `BoqMaterialsModal` — أصناف مسموحة لكل بند BOQ.

---

## 9. التكاليف الفعلية

**ثلاثة تبويبات:** فاتورة مشتريات · مستخلص مقاول · تسوية عهدة  
(لا تستخدم `Purchases.tsx` أو `SubcontractorExtracts.tsx` — مخفية من الشريط)

### موردون

- [ ] إنشاء مورد مواد → COA تحت `21101` (كود 8 أرقام تسلسلي).
- [ ] إنشاء مقاول باطن → COA تحت `21102`.
- [ ] `writeBatch`: مورد + حساب في عملية واحدة.

### فاتورة مشتريات

- [ ] اختيار دائن من ورقة COA (`21101…`) وليس فقط مجموعة `suppliers`.
- [ ] ضريبة وخصم وإضافة؛ ربط بند BOQ.
- [ ] **محلي:** توزيع على عقود + `materialCategoryId`؛ `status: confirmed` يحدّث المخزون.
- [ ] قيد GL متوازن؛ `supplierAccountId` على `purchase_transactions`.
- [ ] تحذير تجاوز ميزانية BOQ (غير مانع).
- [ ] بطاقة ملخص مخزون بعد الحفظ (محلي).

### مستخلص مقاول باطن

- [ ] من تبويب **مستخلص مقاول** (ليس موديول منفصل).
- [ ] دائن من `21102…`.
- [ ] استقطاعات وضرائب؛ قيد متوازن؛ لا تكرار قيود عند تغيير الحالة.

### تسوية عهدة

- [ ] `GLCustodySettlement`: بنود متعددة؛ مركز تكلفة لكل بند.
- [ ] قيود مجمعة per عقد؛ لا كتابة في `purchase_transactions`.
- [ ] رفض تسوية غير متوازنة.

---

## 10. المستخلصات Billing / IPC

- [ ] مسودة لعقد؛ بنود BOQ وكميات/نسب.
- [ ] VAT 14% · ضمان 10% (شامل VAT) · تأمينات 5% · قوى عاملة 0.3% — من `billingDefaults.ts`.
- [ ] مسودة **لا** تنشئ قيد إيراد نشط.
- [ ] تقديم → `recordIPC()` + `billing.transactionId`.
- [ ] اعتماد/سداد دون تكرار القيد.
- [ ] الرجوع لمسودة: `writeBatch` يعطل GL ويمسح `transactionId`.
- [ ] تقدم BOQ يتحدث بعد التقديم/الاعتماد.
- [ ] ترقيم مستخلصات فريد per عقد.

---

## 11. إدارة المخازن (محلي فقط)

> يتطلب `local:api`. بدون SQLite: بانر تحذير وعدم تنفيذ العمليات.

### تبويب الأصناف (Materials)

- [ ] نفس `MaterialsTree` (إضافة / تصدير / استيراد).

### تبويب مخزن المشروع (Balance)

- [ ] اختيار مشروع + عقد تحميل؛ ملخص **project_inventory**: وارد، مصروف، مرتجع، رصيد، محجوز، متاح.
- [ ] فاتورة مشتريات `confirmed` → وارد 100% لمخزن المشروع · GL `12701001` (لا مصروف عقد من الفاتورة).
- [ ] **أمر صرف جديد** → مسودة → **تأكيد** (`consumption-orders/:id/confirm`).
- [ ] التأكيد يحدّث `boq_actual_costs` ويقلل المتاح في مخزن المشروع.
- [ ] **إذن إرجاع** من سجل الصرف → `return-orders/:id/confirm` يعكس BOQ والمخزن.
- [ ] منع صرف أكبر من **متاح مخزن المشروع**.
- [ ] ربط بند BOQ بصنف مسموح فقط (`boq_item_materials`).
- [ ] تقرير حركة المخزن (وارد/صرف/مرتجع) من `project_inventory_movements`.

### تبويب تحويل مشاريع (Project transfers)

- [ ] ربط حساب مخزن **127…** لكل مشروع (من تبويب رصيد المخزن) قبل الاعتماد النهائي.
- [ ] طلب تحويل جديد: يظهر أصناف المصدر ذات رصيد متاح (لا قائمة فارغة عند وجود رصيد).
- [ ] إنشاء طلب: `pending_b` + حجز `quantity_reserved` على بنود المصدر.
- [ ] `approve-b`: انتقال إلى `pending_projects` (صلاحية مشروع الوجهة أو admin/PM).
- [ ] `approve-projects` (PM/admin): حركة مخزن + قيد GL **مدين مخزن الوجهة / دائن مخزن المصدر** · مرجع `PTRF-…` في دفتر اليومية.
- [ ] رفض/إلغاء: تحرير الحجز؛ لا قيد GL.
- [ ] مشروع الوجهة غير موجود في SQLite: يُنشأ تلقائياً عند الإنشاء (بدون خطأ FK).

### تحويلات عقود (legacy — إن وُجدت معلّقة)

- [ ] **لا** طلبات تحويل جديدة بين عقود.
- [ ] إكمال المعلّق فقط: `pending_b` → `pending_projects` → `approved`.

### تبويب السجل (History)

- [ ] سجل أوامر الصرف (مسودة/مؤكد) مع التفاصيل.

### سير عمل متكامل (مواد)

```
شجرة أصناف → ربط BOQ → فاتورة confirmed → مخزن مشروع → (اختياري) تحويل مشروع PTRF → أمر صرف confirm → boq_actual_costs → (اختياري) إرجاع
```

- [ ] **لا** استخدام `POST /api/inventory/consume` (legacy — مُستبدل بأوامر الصرف).

---

## 12. البنوك

### حسابات

- [ ] إنشاء بنك وربط COA تحت `12101…`.

### حركات

- [ ] إيداع / صرف؛ دفعة مقدمة عميل → `ADVANCE_PAYMENT` (التزام).
- [ ] ربط بعقد؛ قيد GL متوازن.
- [ ] كشف بنك: افتتاحي + حركات + ختامي.

### شيكات

- [ ] شيك وارد/صادر؛ تغيير الحالة والأثر المحاسبي في المرحلة الصحيحة.

---

## 13. التقارير

- [ ] **محلي:** التكاليف الفعلية في Overview/Budget = GL (بدون 51101 من فاتورة قديمة) + `boq_actual_costs` للمواد.
- [ ] **محلي:** فاتورة مشتريات مؤكدة لا ترفع مصروف class 5 في Dashboard (وارد 12701 فقط).
- [ ] قائمة الدخل.
- [ ] الميزانية: أصول غير متداولة → متداولة؛ خصوم بنفس الترتيب؛ متوازنة.
- [ ] تحليل تفصيلي / رسوم بيانية (افتراضي مخفي).
- [ ] فلتر مشروع وعقد (عند تعدد العقود).
- [ ] تطابق مع GL لنفس الفترة (مع مراعاة `dataLimits`).
- [ ] تصدير PDF/Excel إن وُجد — عربي وRTL.

---

## 14. تقرير السيولة

> داخل **التقارير** — تبويب السيولة (ليس نافذة منفصلة).

- [ ] رصيد نقد وبنوك = صافي `121…`.
- [ ] المستحق لا ينخفض بسبب الدفعات المقدمة.
- [ ] جدول per عقد: مستخلص، محصل، مقدمات، محتجزات، % تحصيل.
- [ ] تحديث billing/transactions ينعكس (ضمن حد التحميل).

---

## 15. الإعدادات

- [ ] بيانات الشركة `settings/company_info`.
- [ ] إدارة مستخدمين: إنشاء قبل أول دخول بـ UID الصحيح.
- [ ] تعديل دور وصلاحيات وعقود مسندة.
- [ ] حذف/تعطيل مستخدم؛ **محلي:** تعطيل SQLite يمنع جلسة جديدة.
- [ ] عمليات حساسة: `verifyAdministratorIdentity()` (Google popup).
- [ ] **إعدادات العرض** منفصلة عن Settings — لا تكرار ثيم/موديول افتراضي في Settings.

---

## 16. النشاط والتدقيق

- [ ] `activity_logs` عند توفر اللوحة.
- [ ] تتبع مصدر القيد: IPC / فاتورة / بنك / عهدة / صرف مخزون.
- [ ] لا أسرار في Console أو الواجهة.

---

## 17. التكامل بين الوحدات (سيناريو E2E)

نفّذ بالترتيب على عقد واحد (محلي):

- [ ] 1. مشروع + عقد
- [ ] 2. BOQ (3 بنود على الأقل)
- [ ] 3. شجرة أصناف (صنفين) + ربط BOQ↔مواد
- [ ] 4. مورد + فاتورة موزعة `confirmed` + قيد GL
- [ ] 5. رصيد مخزون يظهر الكمية ومتوسط التكلفة
- [ ] 6. أمر صرف مؤكد → `boq_actual_costs` في BOQ
- [ ] 7. مستخلص عميل → تقديم → اعتماد → تحصيل
- [ ] 8. لوحة تحكم + سيولة + ميزانية — أرقام متسقة
- [ ] 9. كل القيود متوازنة؛ لا `transactionId` مكرر على billing

---

## 18. الأداء والتحميل

- [ ] فتح التطبيق والموديولات مع بيانات كثيرة مقبول.
- [ ] بحث الجداول لا يجمّد الواجهة (BOQ/GL طويلة — راقب الحاجة لـ virtualize).
- [ ] عدة نوافذ مفتوحة دون تسرب ذاكرة واضح.
- [ ] **محلي:** `listenQuery` = قراءة لمرة واحدة (لا وميض WebSocket Firestore).
- [ ] حدود `dataLimits` مع تنبيه للمستخدم.

---

## 19. التوافق وتجربة المستخدم

- [ ] لابتوب + شاشة كبيرة؛ RTL بدون قص عناصر.
- [ ] رسائل خطأ مفهومة بالعربية.
- [ ] حقول مبالغ رقمية؛ تواريخ `YYYY-MM-DD`.
- [ ] أزرار خطرة بتأكيد.

---

## 20. الأمان

- [ ] لا بيانات بدون Auth.
- [ ] Firestore rules + API محلي يمنعان قراءة/كتابة غير مصرحة.
- [ ] مستخدم لا يرفع صلاحياته ذاتياً.
- [ ] لا مفاتيح سرية في `dist/` أو git.
- [ ] إدخال رموز خاصة/nosql لا يكسر الواجهة.

---

## 21. حالات فشل مقصودة

- [ ] قطع إنترنت أثناء تحميل Firestore.
- [ ] قطع إنترنت أثناء حفظ IPC/فاتورة.
- [ ] إيقاف `local:api` وفتح المخازن → رسالة واضحة.
- [ ] فتح موديول URL/اختصار بدون صلاحية.
- [ ] قيد غير متوازن؛ كمية صرف > المتاح؛ Excel خاطئ لـ BOQ أو أصناف.
- [ ] مستخلص بدون عقد/بنود.
- [ ] تأكيد أمر صرف بدون ربط BOQ↔مادة.

---

## قرار الاختبار النهائي

- [ ] مقبول للاستخدام العملي
- [ ] مقبول بشرط إصلاح ملاحظات بسيطة
- [ ] إعادة اختبار بعد إصلاحات جوهرية
- [ ] غير مقبول حالياً

**توقيع / تاريخ القرار:**

---

## ملحق: أوامر مساعدة

```bash
npm run lint          # TypeScript frontend
npm run test          # Vitest
cd server && npx tsc --noEmit   # TypeScript backend
npm rebuild better-sqlite3      # بعد تغيير إصدار Node
```

**نشر Firebase (إنتاج):**

```bash
npx firebase-tools deploy --only firestore:indexes,firestore:rules --project gen-lang-client-0599011721
```
